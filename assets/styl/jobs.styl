.jobs
	background #fff
	// position relative
	height 100%
	min-height 100%
	padding 50px 0
	h1
		font-weight 400
		font-size 3em
		margin-bottom 20px
	dt
		height: 200px;
		position: relative;
		overflow: hidden;
	input
		display none