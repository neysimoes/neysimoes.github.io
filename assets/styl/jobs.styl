.jobs
	background: #fff;
	padding: 50px 0;
	color: #000;
	h1
		font-weight 400
		font-size 3em
		margin-bottom 40px
	dt
		height: 200px;
		position: relative;
		overflow: hidden;
		margin-bottom 30px
	input
		display none
	h3, p
		font-family "lato"
		margin-top 5px